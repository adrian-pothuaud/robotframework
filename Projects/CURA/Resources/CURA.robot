*** Setting ***
*** Variable ***
*** Keyword ***
login page is open
    Log    open login page

username ${username} and password ${password} are inserted
    Log    insertion of credentials ${username} and ${password}

credentials are submitted
    Log    submit credentials

bad login popup should appear
    Log    bad login 